import { Routes } from '@angular/router';
import { Home } from './pages/home/home';
import { ImageDetectorComponent } from './pages/image-detector/image-detector';

export const routes: Routes = [
  { path: '', component: ImageDetectorComponent }
];
